
# API Request Tester

A lightweight, clean web application built with Flask for testing RESTful API endpoints. Easily send HTTP requests, configure headers and payloads, and inspect status codes, response headers, and formatted JSON outputs.

## Project Structure

```text
api-tester/
│
├── app.py
├── requirements.txt
├── README.md
├── static/
│   └── style.css
├── templates/
│   └── index.html
└── tests/
    └── test_app.py
